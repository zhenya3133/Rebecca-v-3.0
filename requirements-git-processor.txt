# Git Processor Dependencies
# Основные зависимости для анализа Git репозиториев

# Git операции
GitPython>=3.1.0

# Структуры данных и валидация
pydantic>=1.8.0

# YAML парсинг (опционально, для pyproject.toml)
PyYAML>=5.4.0

# Дополнительные зависимости для расширенного анализа
python-magic>=0.4.27  # Определение типов файлов
chardet>=4.0.0        # Определение кодировки файлов

# Асинхронность (встроен в Python 3.7+, но для полной совместимости)
# asyncio-mqtt>=0.10.0  # Для асинхронных операций (если нужно)

# Опциональные зависимости для специфических функций
# beautifulsoup4>=4.9.0  # Для парсинга HTML документации
# lxml>=4.6.0            # Для XML парсинга (быстрее чем built-in)
# markdown>=3.3.0       # Для обработки Markdown файлов

# Тестирование и разработка
pytest>=6.0.0
pytest-asyncio>=0.15.0
pytest-cov>=2.12.0
mock>=4.0.0

# Документация
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0